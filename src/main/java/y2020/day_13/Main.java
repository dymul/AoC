package y2020.day_13;

public class Main {

    public static void main(String[] args) {
//        String[] in = input;
//
//        int timestamp = Integer.parseInt(in[0]);
//        List<Integer> schedules = Arrays.stream(in[1].split(",")).filter(s -> !s.equals("x")).map(s -> Integer.parseInt(s)).collect(Collectors.toList());
//        Map.Entry<Integer, Integer> entry = schedules.stream()
//                .map(s -> {
//                    int before = timestamp % s;
//                    int after = s - before;
//                    new Map.Entry<>(s, after)
//                    return Map.Entry(s, after);
//                })
//                .min(Comparator.comparingInt(Map.Entry::getValue))
//                .get();
//        System.out.println(entry);
//        System.out.println(entry.getKey()*entry.getValue());


    }

    static String[] input = {
            "1000677",
            "29,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,x,41,x,x,x,x,x,x,x,x,x,661,x,x,x,x,x,x,x,x,x,x,x,x,13,17,x,x,x,x,x,x,x,x,23,x,x,x,x,x,x,x,521,x,x,x,x,x,37,x,x,x,x,x,x,x,x,x,x,x,x,19"
    };

    static String[] in2 = {
            "939",
            "7,13,x,x,59,x,31,19"
    };



}
